name: CI/CD Receitas

on:
  push:
    branches: [ main ]

jobs:
  tests:
    name: Testes automatizados
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Configurar PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: '8.2'
          extensions: mbstring, pdo, pdo_sqlite, sqlite
          tools: composer

      - name: Instalar dependências
        run: composer install --no-interaction --prefer-dist --optimize-autoloader

      - name: Preparar ambiente
        run: |
          cp .env.example .env
          php artisan key:generate
          touch database/database.sqlite

      - name: Executar testes
        run: php artisan test --log-junit junit.xml

      - name: Estatísticas dos testes
        if: always()
        run: |
          TOTAL=$(grep -o 'tests="[0-9]*"' junit.xml | head -1 | grep -o '[0-9]*')
          FALHAS=$(grep -o 'failures="[0-9]*"' junit.xml | head -1 | grep -o '[0-9]*')
          ERROS=$(grep -o 'errors="[0-9]*"' junit.xml | head -1 | grep -o '[0-9]*')
          IGNORADOS=$(grep -o 'skipped="[0-9]*"' junit.xml | head -1 | grep -o '[0-9]*')
          PASSOU=$((TOTAL - FALHAS - ERROS - IGNORADOS))

          echo "## Estatísticas dos Testes" >> $GITHUB_STEP_SUMMARY
          echo "| Métrica | Quantidade |" >> $GITHUB_STEP_SUMMARY
          echo "|---|---:|" >> $GITHUB_STEP_SUMMARY
          echo "| Total | $TOTAL |" >> $GITHUB_STEP_SUMMARY
          echo "| Aprovados | $PASSOU |" >> $GITHUB_STEP_SUMMARY
          echo "| Falhas | $FALHAS |" >> $GITHUB_STEP_SUMMARY
          echo "| Erros | $ERROS |" >> $GITHUB_STEP_SUMMARY
          echo "| Ignorados | $IGNORADOS |" >> $GITHUB_STEP_SUMMARY

          if [ "$TOTAL" -lt 20 ]; then
            exit 1
          fi

      - name: Upload relatório de testes
        uses: actions/upload-artifact@v4
        with:
          name: junit-report
          path: junit.xml

  sonar:
    name: Análise SonarCloud
    runs-on: ubuntu-latest
    needs: tests

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Baixar relatório de testes
        uses: actions/download-artifact@v4
        with:
          name: junit-report

      - name: Executar análise SonarCloud
        uses: SonarSource/sonarqube-scan-action@v7
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}

  deploy_homolog:
    name: Deploy homologação
    runs-on: self-hosted
    needs: sonar
    environment:
      name: homologacao

    steps:
      - name: Atualizar código e reiniciar container homologação
        run: |
          cd /opt/receitas/app-homolog
          git pull origin main
          cd /opt/receitas
          sudo docker compose restart homolog

  deploy_prod:
    name: Deploy produção
    runs-on: self-hosted
    needs: deploy_homolog
    environment:
      name: producao

    steps:
      - name: Atualizar código e reiniciar container produção
        run: |
          cd /opt/receitas/app-prod
          git pull origin main
          cd /opt/receitas
            sudo docker compose restart prod